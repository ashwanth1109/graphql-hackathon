type User {
    id: ID!
    name: String
}

type PaginatedUsers {
    users: [User!]!
    nextToken: String
}

input AddUserInput {
    id: ID!
    name: String
}

type Query {
    allUsers(count: Int, nextToken: String): PaginatedUsers!
}

type Mutation {
    addUser(user: AddUserInput): User!
}